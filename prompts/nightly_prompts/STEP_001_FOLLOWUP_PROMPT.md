# A01-F1 — закрыть пробелы доказательств первого отчёта

## CONTEXT

Внешний инженер проверил A01 — Generator contract по десяти фотографиям ответа. Большая часть контракта восстановлена, но правые столбцы обрезаны, цитаты сокращены, полный Gradle-вызов не виден. Внешний статус A01: PARTIAL. К следующему этапу не переходить.

Исследованный репозиторий: D:/NEWGIT/sql_exporter, ветка FOSQL-12128. Генератор: scripts/gen_exporter_config.py, по предыдущему отчёту 175 строк. Gradle: build.gradle. Полный SHA повторно выведи текстом; не восстанавливай по фотографии. Замена runtime query-exporter под Python 3.12 ещё не выбрана.

## GOAL

Одна задача: предоставить непрерывные исходные фрагменты для проверки уже выполненного A01 и уточнить три спорных утверждения отчёта.

## SCOPE

READ-ONLY. DO NOT MODIFY ANY FILES.

Только применимые инструкции, scripts/gen_exporter_config.py целиком и build.gradle:56–59,189–208. Если точные границы блока сместились, прочитай минимальное продолжение до его окончания. Повторное исследование проекта не требуется.

## DO NOT

Не запускать и не импортировать Python-код проекта, Gradle, exporter, SQL или тесты. Не подключаться к БД/сети, не устанавливать пакеты, не менять git/worktree. Не читать XML, DSN и другие файлы с credentials. Не исследовать DDL, runtime или production. Не исправлять код. Отчёт вернуть сообщением, не создавать файл.

## EXPECTED OUTPUT

1. Git HEAD и ветка текстом, состояние дерева до/после; сообщи, если исследованные файлы изменились относительно A01.
2. Полный scripts/gen_exporter_config.py с исходными номерами строк, без многоточий и пересказа. Разбей на короткие code blocks примерно по 35–45 строк. Строки не обрезай: длинную строку можно перенести визуально, явно указав продолжение того же номера.
3. Дословные build.gradle:56–59,189–208 с номерами строк: переменные, вызов генератора и копирование результата. Секретные литералы, если встретятся, заменить в ответе на [REDACTED], сохранив структуру. Исходники не менять.
4. Три коротких уточнения:
   - metric_types: при прямом индексировании списка из четырёх элементов индексы -1..-4 допустимы. Проверь, есть ли предшествующая валидация; исправь прежнее «вне 0..3 -> IndexError». Не утверждай, что отрицательные значения существуют или разрешены в SQL metadata.
   - Порядок YAML: приведи точный вызов yaml.dump и его параметры. Если гарантия порядка не следует из имеющегося кода, отзови утверждение о сохранении порядка вставки и отметь UNKNOWN. Версию PyYAML сейчас не исследуй.
   - Пробелы watchlist: разграничь strip всей строки и обработку каждого поля после split(';'). Исправь общее «без trim», если оно неточно.
5. HANDOFF: A01-F1; что уточнено; оставшиеся пробелы только текущего scope; команды; подтверждение отсутствия записей и запусков проекта.

Не повторяй широкий отчёт и таблицы A01. Исходные блоки плюс короткие уточнения достаточны.

## ACCEPTANCE CRITERIA

Полный генератор и узкий build-вызов читаемы и привязаны к ревизии; три спорных утверждения исправлены или обоснованы исходными строками. Исследование не расширено; файлы не изменены; секреты не раскрыты.

## STOP CONDITIONS

Если файлы/ревизия существенно изменились, отсутствуют или секретный фрагмент нельзя безопасно показать, верни точный блокер. Не восстанавливай недоступный код по прежнему отчёту. Не начинай A02 автоматически.

## COMMANDS / VALIDATION

Из известного корня репозитория выполни отдельно:

```text
git rev-parse HEAD
git branch --show-current
git --no-optional-locks status --short --untracked-files=normal
```

Для указанных файлов используй штатное read-only чтение агента либо PowerShell Get-Content с номерами строк и отбором нужных диапазонов. rg ранее отсутствовал — не устанавливай его. Не печатай реальные credentials. В конце повтори git status с теми же параметрами; существующие untracked-файлы не трогай.
